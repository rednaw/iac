# Exclude everything not needed for the image build.
# Dockerfile COPYs mise.toml and requirements.txt; keep Dockerfile and those in context.

.git
.github
.devcontainer
docs
ansible
terraform
scripts
tasks
secrets
*.md
LICENSE
.gitignore
.sops.yaml
Taskfile.yml
