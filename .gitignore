# Terraform
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform/
terraform/crash.log

# Ansible
ansible/.ansible/

# SOPS (Secrets OPerationS)
secrets/infrastructure-secrets.yml
.sops.yaml

# Documnents scratchpad
docs/attic

