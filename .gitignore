# SOPS VS Code extension temp files
.decrypted~*

# Terraform
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/.terraform/
terraform/**/crash.log

# Ansible
ansible/.ansible/

# Python
__pycache__/
*.pyc

# Restic restore scratch (restore_from_backup.py when run from repo tree)
prefect/.restore-work/

# Pulled Restic repos (task backup:download)
.backup-repos/

# Mounted app repos under iac/apps/ (devcontainer bind). Fork-only clones stay clean unless using submodules.
apps/

# Fork-only: infra secrets + age recipient pubs for secrets/infra.yml (see docs/future/secrets-and-mounts.md).
# Upstream ignores the tree so merges never touch real secrets; forks `git add -f secrets/` or drop this line locally.
secrets/

.DS_Store
.vscode/
