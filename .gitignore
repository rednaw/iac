# SOPS VS Code extension temp files
.decrypted~*

# Terraform
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/.terraform/
terraform/**/crash.log

# Ansible
ansible/.ansible/

# Python
__pycache__/
*.pyc

# Restic restore scratch (restore_from_backup.py when run from repo tree)
prefect/.restore-work/

# Pulled Restic repos (task backup:download)
.backup-repos/

# Cutover ferry (restic + image tar)
ferry/

# Infra secrets live in the private sibling ../secrets — never in this tree.
# Keep ignoring secrets/ so an accidental local copy is not committed.
secrets/

.DS_Store
.vscode/

# Host skills bind-mounted in the Dev Container
.cursor/skills/
