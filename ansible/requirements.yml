# Ansible Collection Requirements
# Installed automatically by platform:configure:bootstrap / platform:configure:apply / workflow:deploy.
# Versions are pinned for reproducibility. Renovate will create PRs for updates.

collections:
  - name: ansible.posix
    version: "2.2.0"
  - name: community.docker
    version: "5.2.1"
  - name: community.general
    version: "13.1.0"
