# Ansible Collection Requirements
# Install with: task ansible:install
# Versions are pinned for reproducibility. Renovate will create PRs for updates.

collections:
  - name: ansible.posix
    version: "2.1.0"
  - name: community.docker
    version: "5.0.6"
  - name: community.general
    version: "11.4.0"
