# Fail2ban filter: Traefik access log – known bad/scanner paths
# Used by jail traefik-badbots. Log format: common (NCSA).
# Path-based (no User-Agent in common format).

[Definition]
failregex = ^<HOST> -.*"(GET|POST|HEAD).*(wp-login\.php|wp-admin|xmlrpc\.php|\.env|config\.php|phpmyadmin|\.git/|/shell|/eval|/admin/config\.php).*HTTP.*" .*$
ignoreregex =
