# Fail2ban filter: Traefik access log – 403 Forbidden
# Used by jail traefik-forbidden. Log format: common (NCSA).

[Definition]
failregex = ^<HOST> -.*"(GET|POST|HEAD).*HTTP.*" 403 .*$
ignoreregex =
