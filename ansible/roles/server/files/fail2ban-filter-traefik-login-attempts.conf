# Fail2ban filter: Traefik access log – failed /api/auth/login (401|403)
# Used by jail traefik-login-attempts. Log format: common (NCSA).

[Definition]
failregex = ^<HOST> -.*"/api/auth/login.*HTTP.*" (401|403) .*$
ignoreregex =
