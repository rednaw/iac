# yaml-language-server: $schema=https://json.schemastore.org/traefik-v2.json
# Routers for app_domains so Traefik requests Let's Encrypt certs (TLS pre-warm)
# even when the app container is not running. Priority -1 so the app's Docker router wins when deployed.
# Domains from iac.yml app_domains (unencrypted).
http:
  routers:
    app-host:
      rule: "Host(`{{ traefik_app_domains | join('`) || Host(`') }}`)"
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      priority: -1
      service: noop@internal
